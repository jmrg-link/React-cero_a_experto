describe("Pruebas en archivo demo.test.js", () => {
    // 1.
    test("deben ser iguales los strings ", () => {
    // 1. inicializacion
    const msj1 = "hola tu";

    // 2. estimulo
    const msj2 = `hola tu`;

    // 3. obersvar el comportamiento
    expect(msj1).toBe(msj2);
  });

    // 2.
    

});
